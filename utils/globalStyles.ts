import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    borderRadius: 0,
    marginBottom: 5,
    marginTop: 5,
  },
  buttonText: { fontSize: 16 },
  paragraph: {
    paddingBottom: 10,
    lineHeight: 20
  },
});